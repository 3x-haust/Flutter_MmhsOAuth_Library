name: mirim_oauth_flutter
description: Flutter library for Mirim OAuth authentication
version: 1.0.4
homepage: https://github.com/3x-haust/Flutter_MmhsOAuth_Library

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  flutter_secure_storage: ^9.2.4
  flutter_web_auth_2: ^4.1.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
